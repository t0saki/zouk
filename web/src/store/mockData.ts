// mockData: Commented out - replaced by live server data via WebSocket + REST API
// This file is preserved for reference but no longer used.

/*
import type { Workspace, Profile, Channel, Message, ReactionGroup } from '../types';

// ... all mock data was here ...
*/

export {};
