// ProfileCard: Commented out - no protocol support in toy-slock-server
// Profile card functionality (presence, status, DM actions) is not part of the current server protocol.
// This component is preserved for future implementation.

/*
import { X, MessageSquare, Phone, Video, Clock, Mail } from 'lucide-react';
import { useApp } from '../store/AppContext';

export default function ProfileCard() {
  // ... profile card implementation would go here
  return null;
}
*/

export default function ProfileCard() {
  return null;
}
